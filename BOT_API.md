# مستندات API ربات تلگرام

این مستند به شرح دستورات، دکمه‌ها و جریان‌های کاری اصلی ربات می‌پردازد.

## 1. دستورات اصلی

### 1.1. `/start`
- **توضیح:** شروع تعامل با ربات یا بازگشت به منوی اصلی.
- **عملکرد:** نمایش پیام خوشامدگویی و منوی اصلی دکمه‌ها بر اساس وضعیت کاربر (عادی، ادمین، نماینده) و تنظیمات ربات.

<!-- سایر دستورات در اینجا اضافه خواهند شد -->

## 2. منوی اصلی و دکمه‌ها

منوی اصلی شامل دکمه‌های زیر است که بسته به وضعیت کاربر و تنظیمات ربات ممکن است متفاوت باشد:

- **🛒 خرید کانفیگ (`buySubscription`)**: شروع فرآیند خرید کانفیگ جدید.
- **📱 کانفیگ های من (`mySubscriptions` / `agentConfigsList`)**: نمایش لیست کانفیگ‌های خریداری شده توسط کاربر/نماینده.
- **🎁 دریافت اکانت تست (`getTestAccount`)**: (در صورت فعال بودن) دریافت یک اکانت تست رایگان.
- **✅ 💳 شارژ کیف پول (`increaseMyWallet`)**: شروع فرآیند شارژ کیف پول کاربر.
- **🏆 زیر مجموعه گیری (`inviteFriends`)**: نمایش لینک دعوت و اطلاعات مربوط به پاداش دعوت.
- **🧑‍💼 حساب کاربری (`myInfo`)**: نمایش اطلاعات حساب کاربر شامل موجودی، تعداد خریدها و ...
- **🧑‍💼 درخواست نمایندگی (`requestAgency`)**: (در صورت فعال بودن و کاربر عادی) ارسال درخواست برای تبدیل شدن به نماینده.
- **➰ پنل همکاری (`agencySettings`)**: (مخصوص نمایندگان) ورود به منوی مدیریت نمایندگی.
    - **➕ خرید تکی (`agentOneBuy`)**: خرید تکی کانفیگ توسط نماینده.
    - **♾ خرید انبوه (`agentMuchBuy`)**: خرید انبوه کانفیگ توسط نماینده.
- **🧩 آموزش اتصال (`reciveApplications`)**: نمایش لینک دانلود نرم‌افزارهای مورد نیاز.
- **📨 تیکت های من (`supportSection`)**: ورود به بخش پشتیبانی و مدیریت تیکت‌ها.
- **🪫 مشخصات کانفیگ (`showUUIDLeft`)**: (در صورت فعال بودن) امکان جستجو و مشاهده مشخصات یک کانفیگ با UUID.
- **مدیریت ربات ⚙️ (`managePanel`)**: (مخصوص ادمین) ورود به پنل مدیریت ربات.
- **دکمه‌های داینامیک**: دکمه‌هایی که توسط ادمین از طریق پنل مدیریت تعریف شده‌اند و به `showMainButtonAns<ID>` اشاره می‌کنند.

<!-- شرح دقیق‌تر هر دکمه و جریان کاری مربوطه در اینجا یا بخش‌های جداگانه اضافه خواهد شد -->

## 3. جریان‌های کاری اصلی (مثال‌ها)

### 3.1. خرید کانفیگ
1. کاربر دکمه "خرید کانفیگ" را انتخاب می‌کند.
2. ربات لیستی از سرورها/لوکیشن‌های موجود را نمایش می‌دهد.
3. کاربر سرور مورد نظر خود را انتخاب می‌کند.
4. ربات لیست پلن‌های موجود برای آن سرور را نمایش می‌دهد.
5. کاربر پلن مورد نظر خود را انتخاب می‌کند.
6. ربات جزئیات پلن (حجم، مدت زمان، قیمت) و گزینه‌های پرداخت (کیف پول، درگاه پرداخت، کارت به کارت) را نمایش می‌دهد.
7. کاربر روش پرداخت را انتخاب و پرداخت را تکمیل می‌کند.
8. پس از تایید پرداخت، ربات کانفیگ (یا لینک‌های کانفیگ و لینک اشتراک) را برای کاربر ارسال می‌کند.

### 3.2. شارژ کیف پول
1. کاربر دکمه "شارژ کیف پول" را انتخاب می‌کند.
2. ربات از کاربر مبلغ مورد نظر برای شارژ را سوال می‌کند.
3. کاربر مبلغ را وارد می‌کند.
4. ربات گزینه‌های پرداخت را نمایش می‌دهد.
5. پس از پرداخت موفق، موجودی کیف پول کاربر در پایگاه داده به‌روزرسانی شده و پیام تایید برای کاربر ارسال می‌شود.

<!-- سایر جریان‌های کاری مهم مانند تمدید سرویس، درخواست نمایندگی، استفاده از اکانت تست، سیستم تیکتینگ و ... در اینجا مستند خواهند شد. -->

## 4. فرمت داده‌های Callback (Callback Data Format) - برای توسعه‌دهندگان

داده‌های `callback_data` برای دکمه‌های اینلاین معمولاً از یک فرمت مشخص پیروی می‌کنند تا پردازش آن‌ها در سمت سرور ساده‌تر شود. مثال:
- `actionName` (مانند `buySubscription`, `myInfo`)
- `actionName<ID>` (مانند `orderDetails123`, `selectServer45`)
- `actionName<ID1>_<ID2>` (مانند `selectCategory3_45` که می‌تواند به معنی انتخاب دسته 3 برای سرور 45 باشد)

این فرمت به کنترلر کمک می‌کند تا عملیات و پارامترهای لازم را به درستی تشخیص دهد. مستندات دقیق‌تر برای هر مجموعه از دکمه‌ها در بخش مربوط به خود ارائه خواهد شد.
