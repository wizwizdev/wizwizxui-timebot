# مستندات نقاط پایانی HTTP API

این مستند، نقاط پایانی HTTP API پروژه را که عمدتاً برای تعاملات سیستمی مانند پرداخت و ارائه لینک اشتراک استفاده می‌شوند، شرح می‌دهد.

## 1. API پرداخت (مسیر `/pay/`)

این API برای مدیریت فرآیند پرداخت از طریق درگاه‌های مختلف استفاده می‌شود.

### 1.1. شروع فرآیند پرداخت
- **مسیر:** `GET /pay/`
- **پارامترهای Query:**
    - `gateway_name` (string, required): نام درگاه پرداخت. مقادیر ممکن فعلی در کد PHP شامل `nowpayment`, `zarinpal`, `nextpay` هستند. سایر مقادیر مانند `weswap` یا `cartToCart` یا `tronwallet` از طریق callback مستقیم در تلگرام هندل می‌شوند و احتمالاً نقطه پایانی HTTP مجزا برای شروع ندارند مگر اینکه مستقیماً از طریق URL فراخوانی شوند (که در این صورت باید پارامتر مشابهی داشته باشند).
    - `hash_id` (string, required): شناسه یکتای پرداخت که در جدول `pays` (ستون `hash_id`) قبل از هدایت به این API ایجاد و ذخیره شده است.
- **توضیح:** این نقطه پایانی کاربر را به صفحه پرداخت درگاه مشخص شده هدایت می‌کند یا فرم لازم برای پرداخت را آماده می‌کند.
- **مثال:** `GET /pay/?gateway_name=zarinpal&hash_id=a1b2c3d4e5f6`
- **عملکرد سرور:**
    1. `hash_id` و `gateway_name` را از query string دریافت می‌کند.
    2. اطلاعات پرداخت (مبلغ، نوع پرداخت و ...) را از جدول `pays` بر اساس `hash_id` بازیابی می‌کند.
    3. بررسی می‌کند که آیا وضعیت پرداخت `pending` است یا خیر.
    4. بر اساس `gateway_name`، درخواست لازم را به API درگاه پرداخت ارسال می‌کند (مثلاً برای دریافت توکن پرداخت یا URL هدایت).
    5. `payid` (شناسه تراکنش درگاه) دریافت شده از درگاه را در رکورد مربوطه در جدول `pays` ذخیره می‌کند.
    6. کاربر را به URL درگاه پرداخت هدایت می‌کند یا اطلاعات لازم برای پرداخت (مانند آدرس ولت برای ارز دیجیتال) را نمایش می‌دهد.
- **پاسخ موفق:**
    - HTTP `302 Found` با هدر `Location` به آدرس درگاه پرداخت.
    - یا یک صفحه HTML که اطلاعات پرداخت را نمایش می‌دهد (مثلاً برای پرداخت‌های ارز دیجیتال).
- **پاسخ خطا:**
    - `400 Bad Request`: اگر پارامترهای لازم (`hash_id`, `gateway_name`) موجود نباشند یا نامعتبر باشند.
    - `404 Not Found`: اگر `hash_id` در پایگاه داده یافت نشود یا وضعیت آن `pending` نباشد.
    - `500 Internal Server Error`: در صورت بروز خطا در ارتباط با API درگاه پرداخت.

### 1.2. بازگشت از درگاه پرداخت (Callback)
- **مسیر:** `GET /pay/back` یا `POST /pay/back` (بسته به درگاه و نوع اطلاع‌رسانی مانند IPN)
- **پارامترهای Query/Body:** این پارامترها کاملاً به درگاه پرداخت بستگی دارد.
    - **مثال برای زرین‌پال (GET):** `?Authority=<authority_code>&Status=<OK_or_NOK>&hash_id=<original_hash_id>`
    - **مثال برای نکست‌پی (GET):** `?trans_id=<transaction_id>&order_id=<local_order_id_from_pays_table>&amount=<amount>&np_status=<status_code>&hash_id=<original_hash_id>`
    - **مثال برای NowPayments (معمولاً POST IPN):** Body شامل `payment_id`, `payment_status`, `order_id` (که همان `hash_id` ماست) و ...
- **توضیح:** این نقطه پایانی توسط درگاه پرداخت پس از اتمام (موفق یا ناموفق) عملیات پرداخت توسط کاربر، فراخوانی می‌شود. همچنین ممکن است برای IPN (Instant Payment Notification) توسط برخی درگاه‌ها استفاده شود.
- **عملکرد سرور:**
    1. پارامترهای بازگشتی از درگاه را دریافت و اعتبارسنجی می‌کند.
    2. `hash_id` یا `payid` را برای شناسایی رکورد پرداخت در جدول `pays` استخراج می‌کند.
    3. در صورت نیاز، وضعیت تراکنش را با ارسال درخواست "Verify" به API درگاه پرداخت، تأیید نهایی می‌کند.
    4. **در صورت موفقیت آمیز بودن تراکنش:**
        - وضعیت رکورد در جدول `pays` به `approved` (یا معادل آن) تغییر می‌کند.
        - منطق مربوط به نوع پرداخت اجرا می‌شود:
            - **`INCREASE_WALLET`**: موجودی کاربر در جدول `users` افزایش می‌یابد.
            - **`BUY_SUB` / `RENEW_ACCOUNT` / `INCREASE_DAY` / `INCREASE_VOLUME`**: کانفیگ مربوطه ایجاد/تمدید/به‌روزرسانی می‌شود (از طریق توابع مربوط به XUI/Marzban). رکورد در `orders_list` ایجاد یا به‌روزرسانی می‌شود.
        - پیام موفقیت از طریق ربات تلگرام به کاربر ارسال می‌شود.
        - نوتیفیکیشن به ادمین ارسال می‌شود.
    5. **در صورت ناموفق بودن تراکنش:**
        - وضعیت رکورد در جدول `pays` به `declined` (یا معادل آن) تغییر می‌کند.
        - پیام خطا/لغو به کاربر از طریق ربات تلگرام ارسال می‌شود.
- **پاسخ به درگاه (برای IPN):** `200 OK` برای تأیید دریافت IPN.
- **پاسخ به کاربر (برای Redirect):** یک صفحه HTML که نتیجه تراکنش (موفقیت یا خطا) را به کاربر نمایش می‌دهد و او را راهنمایی می‌کند که به ربات بازگردد.

## 2. API لینک اشتراک (مسیر `/settings/`)

### 2.1. دریافت محتوای لینک اشتراک
- **مسیر:** `GET /settings/subLink`
- **پارامتر Query:**
    - `token` (string, required): توکن یکتای کاربر برای لینک اشتراک (از ستون `token` در جدول `orders_list` یا یک جدول جداگانه برای توکن‌های اشتراک).
- **توضیح:** محتوای مورد نیاز برای کلاینت‌های V2Ray/Xray و سایر کلاینت‌های سازگار را بر اساس توکن کاربر ارائه می‌دهد.
- **عملکرد سرور:**
    1. توکن را از query string دریافت و اعتبارسنجی می‌کند.
    2. کاربر یا لیست سفارشات فعال مرتبط با توکن را از پایگاه داده بازیابی می‌کند. (در کد PHP فعلی، به نظر می‌رسد هر `orders_list` یک توکن منحصر به فرد برای اشتراک لینک‌های همان سفارش دارد، یا یک توکن کلی برای کاربر وجود دارد که تمام کانفیگ‌های فعال او را برمی‌گرداند. این باید در پیاده‌سازی Node.js مشخص شود).
    3. لیست لینک‌های کانفیگ (از ستون `link` در `orders_list` که JSON است) را استخراج می‌کند.
    4. تمام لینک‌های استخراج شده را در خطوط جداگانه ترکیب می‌کند.
- **پاسخ موفق:**
    - `Content-Type: text/plain; charset=utf-8`
    - Body: لیستی از لینک‌های کانفیگ، هر کدام در یک خط جدید.
    ```
    vmess://...
    vless://...
    trojan://...
    ```
- **پاسخ خطا:**
    - `400 Bad Request`: اگر پارامتر `token` ارائه نشده باشد.
    - `404 Not Found`: اگر توکن نامعتبر باشد یا هیچ کانفیگ فعالی برای آن توکن یافت نشود.
    - `500 Internal Server Error`: در صورت بروز خطای داخلی.

## 3. API وب‌هوک تلگرام (اختیاری، در صورت عدم استفاده از Polling)

- **مسیر:** `POST /api/telegram/webhook` (یا هر مسیری که برای تلگرام تنظیم شود)
- **توضیح:** این نقطه پایانی برای دریافت آپدیت‌های تلگرام در حالت وب‌هوک استفاده می‌شود.
- **Body درخواست:** آبجکت `Update` تلگرام به صورت JSON.
- **عملکرد سرور:**
    1. آپدیت JSON را از body درخواست دریافت می‌کند.
    2. آپدیت را به تابع `telegramController.handleUpdate` برای پردازش ارسال می‌کند.
- **پاسخ موفق:** `200 OK` (پاسخ خالی و سریع به سرور تلگرام برای تأیید دریافت آپدیت).
- **پاسخ خطا:** سرور باید خطاها را لاگ کند. تلگرام در صورت عدم دریافت پاسخ `200 OK` در مدت زمان مشخص، تلاش برای ارسال مجدد آپدیت خواهد کرد.

---
**نکات مهم برای مستندسازی:**
- **Authentication/Authorization:** اگر هر یک از این APIها نیاز به احراز هویت دارند (مثلاً برای ادمین‌ها)، باید در مستندات ذکر شود.
- **Rate Limiting:** اگر محدودیت نرخ برای فراخوانی APIها وجود دارد، باید مشخص شود.
- **Error Codes:** لیست کدهای خطای ممکن و معانی آن‌ها باید ارائه شود.
- **مثال‌های کامل Request/Response:** ارائه مثال‌های کامل برای درخواست و پاسخ می‌تواند به درک بهتر API کمک کند.

این مستندات به مرور و با تکمیل پیاده‌سازی هر بخش، دقیق‌تر و کامل‌تر خواهند شد.
